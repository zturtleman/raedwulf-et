#!/bin/sh
# Needed to make symlinks/shortcuts work.
# the binaries must run with correct working directory
cd "${CMAKE_INSTALL_PREFIX}/${wolfet2_INSTALL_NAME}"
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:.
exec ./et.${ARCH} "$@"
